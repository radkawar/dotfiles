complete -c toggle_prompt -f
complete -c toggle_prompt -l global -d "Set prompt style globally (persists across sessions)"
complete -c toggle_prompt -a "two-line single-line minimal" -d "Prompt style" 